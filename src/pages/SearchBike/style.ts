import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    inputContainer: {
        borderWidth: 2,
        borderRadius: 10,
        marginVertical: 15,
        flexDirection: 'row',
        alignItems: 'center'
    }
})

export default styles;