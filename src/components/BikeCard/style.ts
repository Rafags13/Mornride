import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    cardContainer: {
        gap: 10,
        width: 200
    },
    amountOnStockText: {
        fontSize: 12,
        color: 'red',
        fontWeight: '500',
    }
})

export default styles;