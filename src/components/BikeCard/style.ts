import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    cardContainer: {
        // padding: 10,
        gap: 5,
    },
    amountOnStockText: {
        fontSize: 12,
        color: 'red',
        fontWeight: '500',
    }
})

export default styles;