import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    image: {
        flex: 1,
        width: '100%',
        height: 140,
        resizeMode: 'contain',
    }
})

export default styles;